# Optional. If unset, decoy generation automatically falls back to a
# deterministic, safe decoy name — the app works fully without this.
# Never commit a real key here or anywhere else in the repo.
ANTHROPIC_API_KEY=
